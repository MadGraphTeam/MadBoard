// Column headers in these tables are full sentences ("Unweighting efficiency
// (after cuts)"), which a narrow column cuts off with no way to read it. Let
// them wrap over the taller header row instead.
export const WRAPPED_HEADER_SX = {
  "& .MuiDataGrid-columnHeaderTitle": {
    whiteSpace: "normal",
    lineHeight: 1.3,
  },
};

export const HEADER_HEIGHT = 88;
